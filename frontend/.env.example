# ICS Cybersecurity Platform Frontend Environment Variables
# Copy this file to .env and update with your actual values

# Admin Authentication - CRITICAL: Change this in production!
VITE_ADMIN_PASSWORD=your-secure-admin-password-here

# API Configuration
VITE_API_BASE_URL=http://localhost:8000

# Application Settings
VITE_APP_NAME=ICS Cybersecurity Platform
VITE_APP_VERSION=1.0.0

# Security Settings
VITE_SESSION_TIMEOUT=1800000
# Note: Never store sensitive secrets in frontend environment variables
# Frontend env vars are visible to users - use backend API for sensitive operations